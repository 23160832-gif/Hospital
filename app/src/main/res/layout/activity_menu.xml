<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fillViewport="true"
    android:background="@android:color/white">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="24dp"
        android:gravity="center_horizontal">

        <!-- Encabezado -->
        <TextView
            android:id="@+id/tvTituloMenu"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Menú Principal - Hospital"
            android:textSize="28sp"
            android:textStyle="bold"
            android:textColor="@android:color/black"
            android:layout_marginTop="32dp"
            android:layout_marginBottom="48dp" />

        <!-- Botón: Monitoreo WiFi -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnWifi"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Monitoreo WiFi"
            android:textAllCaps="false"
            android:padding="14dp"
            app:cornerRadius="8dp"
            android:layout_marginBottom="16dp" />

        <!-- Botón: Ubicación GPS -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnUbicacion"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Ubicación GPS"
            android:textAllCaps="false"
            android:padding="14dp"
            app:cornerRadius="8dp"
            android:layout_marginBottom="16dp" />

        <!-- Botón: Registrar Paciente / Consulta -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnRegistrarPaciente"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Registrar Paciente / Consulta"
            android:textAllCaps="false"
            android:padding="14dp"
            app:cornerRadius="8dp"
            android:layout_marginBottom="16dp" />

        <!-- Botón: Lista de Pacientes -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnListaPacientes"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Lista de Pacientes"
            android:textAllCaps="false"
            android:padding="14dp"
            app:cornerRadius="8dp"
            android:layout_marginBottom="16dp" />

        <!-- Botón: Visualizar Base de Datos -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnVerBaseDatos"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Visualizar Base de Datos"
            android:textAllCaps="false"
            android:padding="14dp"
            app:cornerRadius="8dp"
            android:layout_marginBottom="16dp" />

        <!-- Botón: Cerrar Sesión (estilo secundario o de alerta) -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnCerrarSesion"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Cerrar Sesión"
            android:textAllCaps="false"
            android:padding="14dp"
            app:cornerRadius="8dp"
            style="@style/Widget.MaterialComponents.Button.OutlinedButton"
            android:layout_marginTop="16dp"
            android:layout_marginBottom="32dp" />

    </LinearLayout>
</ScrollView>